# Runtime dependency lock for Ubuntu 24.04 / Python 3.11.
# Direct requirements remain the reviewed application pins; transitive versions are
# pinned to the exact set observed in successful GitHub Actions on 2026-09-08 so a
# deployment cannot silently resolve newer dependency code beneath the same release.

# Direct application dependencies
httpx==0.27.2
python-dotenv==1.0.1
pydantic==2.9.2
pydantic-settings==2.5.2
fastapi==0.115.0
uvicorn[standard]==0.30.6
websockets==13.1

# Transitive runtime dependencies resolved by the reviewed Python 3.11 environment
annotated-types==0.8.0
anyio==4.15.1
certifi==2026.7.22
click==8.5.0
h11==0.16.0
httpcore==1.0.9
httptools==0.8.0
idna==3.19
PyYAML==6.0.3
sniffio==1.3.1
starlette==0.38.6
typing-extensions==4.16.0
uvloop==0.22.1
watchfiles==1.2.0
